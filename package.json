{
  "name": "fabula-creator-root",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "bun tauri dev",
    "build": "bun build:windows && bun build:linux",
    "build:linux": "bun tauri build",
    "build:windows": "./build-windows.sh",
    "test": "bun test && cd cargo && cargo test"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
